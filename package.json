{
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^6.9.0"
  },
  "name": "cterprojects",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build":"cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroky-postbuild": "npm run install-client && npm run build",
    "start": "node server/server.js",
    "client":"cd client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"

  },
  "engines": {
    "node": "16.x"
  },
  "author": "cterprojects",
  "license": "ISC",
  "description": ""
}
